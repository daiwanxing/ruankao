---
title: 6.2 信息安全的主要技术和措施
description: 掌握身份认证、访问控制、入侵检测、防火墙、网闸、防病毒、数据加密等主要信息安全技术
sidebar:
  order: 2
  badge:
    text: 重点
    variant: tip
---

import MemoryCard from '../../../components/MemoryCard.astro';
import ExerciseBox from '../../../components/ExerciseBox.astro';
import { Card } from '@astrojs/starlight/components';

## 一、教学目标

掌握身份认证、访问控制、入侵检测、防火墙、网闸、防病毒、数据加密等主要信息安全技术的原理和应用。

## 二、核心讲解

### 1. 身份认证

**分类**：
- **用户与主机间的认证**
- **主机与主机之间的认证**

**认证措施**：

| 认证方式 | 说明 |
|---------|------|
| 静态密码 | 传统的用户名+密码组合 |
| 智能卡 | 物理卡片认证 |
| 短信密码 | 通过短信发送验证码 |
| 动态口令 | 定期变化的密码 |
| USB Key | USB硬件设备认证 |
| 生物识别 | 指纹、虹膜、人脸识别等生物特征进行身份认证 |
| 双因素认证 | 两种认证方式结合（如密码+短信验证码） |

### 2. 访问控制

**基本概念**：
防止对任何资源进行未授权的访问，用户是主体，文件是客体，读取文件操作是请求。

**访问控制机制**：

| 机制类型 | 控制方式 | 灵活性 | 安全性 | 记忆要点 |
|---------|----------|--------|--------|----------|
| **自主访问控制（DAC）** | 让客体的所有者来定义访问控制规则 | 高 | 中 | 所有者决定 |
| **基于角色的访问控制（Role-BAC）** | 将主体划分为不同的角色，然后对每个角色的权限进行定义 | 中 | 高 | 角色管理 |
| **基于规则的访问控制（Rule-BAC）** | 制定某种规则，将主体、请求和客体的信息结合起来进行判定 | 中 | 高 | 规则判定 |
| **强制访问控制（MAC）** | 一种基于安全级别标签的访问控制策略 | 低 | 最高 | 强制执行 |

### 3. 入侵检测系统（IDS）

**分类**：
- **实时入侵检测**：在网络连接过程中进行，一旦发现入侵迹象立即断开入侵者与主机的连接，并收集证据和实施数据恢复
- **事后入侵检测**：由具有网络安全专业知识的网络管理人员定期或不定期进行，不具有实时性

### 4. 防火墙

**定义**：
由软件和硬件设备组合而成的，在内部网和外部网之间、专用网与公共网之间的界面上构造的保护屏障。

**组成**：服务访问规则、验证工具、包过滤、应用网关

**防火墙分类**：
- 基于路由器的防火墙
- 用户化的防火墙工具组件
- 建立在通用操作系统上的防火墙
- 具有安全操作系统的防火墙

**基本特性**：
1. 所有网络数据流都必须经过防火墙
2. 只有符合安全策略的数据流才能通过防火墙
3. 自身应具有非常强的抗攻击免疫力
4. 应用层防火墙具备更细致的防护能力
5. 数据库防火墙具有针对数据库恶意攻击的阻断能力

### 5. 网闸（网络隔离技术）

**定义**：
网络隔离技术，由两套各自独立的系统分别连接安全和非安全的网络，两套系统之间通过网闸进行信息摆渡，保证两套系统之间没有直接的物理通路。

**产品方案**：

| 方案类型 | 说明 |
|---------|------|
| **独立网络方案** | 内部网络和外部网络物理断开。两个网络之间如有数据交换需要，则采用人工操作 |
| **终端级解决方案** | 双主板，双硬盘型；单主板，双硬盘型；单主板，单硬盘型 |

### 6. 防病毒

**防病毒策略**：
- **拒绝访问能力**
- **病毒检测能力**
- **控制病毒传播的能力**
- **清除能力**
- **恢复能力**
- **替代操作**

### 7. 数据加密技术

**加密原理**：
将一个信息（明文）经过加密钥匙及加密函数转换，变成无意义的密文，接收方经过解密函数、解密钥匙还原成明文。

**加密算法分类**：

| 算法类型 | 说明 | 典型算法 |
|---------|------|----------|
| **专用密钥（对称密钥）** | 加密和解密时使用同一个密钥 | DES、Kerberos |
| **公开密钥（非对称密钥）** | 加密和解密时使用两个不同的密钥 | RSA |

**数字签名**：
- **作用**：解决伪造、抵赖、冒充和篡改等问题
- **技术**：采用非对称加密技术（如RSA）

<MemoryCard
  title="访问控制四机制"
  formula="DAC（自主）+ Role-BAC（角色）+ Rule-BAC（规则）+ MAC（强制）"
  importance={5}
  category="formula"
/>

## 三、通俗例子

<Card title="身份认证例子" icon="information">
就像进公司要刷卡（门禁卡），这就是身份认证。刷卡后系统确认你是员工才让你进入。
</Card>

<Card title="访问控制例子" icon="information">
就像图书馆的管理系统：
- 普通学生只能借阅普通书籍（自主访问控制）
- 研究生可以进入研究生阅览室（基于角色的访问控制）
- 只有管理员可以进入书库（强制访问控制）
</Card>

<Card title="防火墙例子" icon="information">
就像小区的大门，保安（防火墙）检查每个进出的人：
- 外来人员需要登记（数据包过滤）
- 送货员只能送到门口不能进小区（应用层控制）
</Card>

<Card title="网闸例子" icon="information">
就像两个独立房间，中间有一个传递窗口，房间A的人和房间B的人不能直接接触，只能通过窗口传递物品。
</Card>

<Card title="对称加密例子" icon="information">
就像用同一把钥匙锁门和开门，简单快捷但钥匙丢了就危险了。
</Card>

<Card title="非对称加密例子" icon="information">
就像邮箱系统，每个人有公钥（公开的邮箱地址）和私钥（只有自己知道的密码），别人用你的公钥发邮件，你用私钥解密。
</Card>

## 四、知识点速记

| 知识点 | 关键要点 | 记忆口诀 |
|--------|----------|----------|
| **身份认证两类型** | 用户与主机、主机与主机 | 两认证 |
| **访问控制四机制** | DAC、Role-BAC、Rule-BAC、MAC | 四机制 |
| **IDS两类型** | 实时检测、事后检测 | 两检测 |
| **防火墙四组成** | 服务访问规则、验证工具、包过滤、应用网关 | 四组成 |
| **网闸两方案** | 独立网络方案、终端级解决方案 | 两方案 |
| **加密两类型** | 对称加密（DES、Kerberos）、非对称加密（RSA） | 两加密 |
| **数字签名** | 解决伪造、抵赖、冒充、篡改问题 | 签名 |
| **防病毒六策略** | 拒绝访问、检测、控制传播、清除、恢复、替代操作 | 六策略 |
| **主体-客体关系** | 用户（主体）访问文件（客体） | 关系 |
| **防火墙三特性** | 必经之路、策略控制、强抗攻击性 | 三特性 |

## 五、课后作业

### 课堂测验

<ExerciseBox
  question="以下属于生物识别的是（ ）"
  options={[
    'A. 密码',
    'B. 指纹',
    'C. 智能卡',
    'D. USB Key'
  ]}
  answer="B"
  explanation="指纹属于生物识别技术。"
/>

<ExerciseBox
  question="让客体的所有者来定义访问控制规则，这是指（ ）"
  options={[
    'A. DAC',
    'B. Role-BAC',
    'C. Rule-BAC',
    'D. MAC'
  ]}
  answer="A"
  explanation="DAC（自主访问控制）让客体的所有者来定义访问控制规则。"
/>

<ExerciseBox
  question="防火墙的基本特性不包括（ ）"
  options={[
    'A. 所有网络数据流都必须经过防火墙',
    'B. 只有符合安全策略的数据流才能通过',
    'C. 可以防范所有病毒',
    'D. 自身应具有非常强的抗攻击免疫力'
  ]}
  answer="C"
  explanation="防火墙不能防范所有病毒，病毒防范需要配合防病毒软件。"
/>

<ExerciseBox
  question="以下属于对称加密算法的是（ ）"
  options={[
    'A. RSA',
    'B. DES',
    'C. 数字签名',
    'D. 虹膜认证'
  ]}
  answer="B"
  explanation="DES是对称加密算法，RSA是非对称加密算法。"
/>

<ExerciseBox
  question="基于安全级别标签的访问控制策略是（ ）"
  options={[
    'A. DAC',
    'B. Role-BAC',
    'C. Rule-BAC',
    'D. MAC'
  ]}
  answer="D"
  explanation="MAC（强制访问控制）是一种基于安全级别标签的访问控制策略。"
/>

<ExerciseBox
  question="网闸的主要特点是（ ）"
  options={[
    'A. 高速传输数据',
    'B. 两套系统之间没有直接的物理通路',
    'C. 降低系统成本',
    'D. 提高系统性能'
  ]}
  answer="B"
  explanation="网闸的主要特点是通过信息摆渡，保证两套系统之间没有直接的物理通路。"
/>

<ExerciseBox
  question="以下哪种认证方式不属于双因素认证（ ）"
  options={[
    'A. 用户名+密码',
    'B. 指纹+密码',
    'C. 智能卡+密码',
    'D. USB Key+密码'
  ]}
  answer="A"
  explanation="用户名+密码是单因素认证，指纹+密码、智能卡+密码、USB Key+密码都是双因素认证。"
/>

<ExerciseBox
  question="防火墙的组成不包括（ ）"
  options={[
    'A. 服务访问规则',
    'B. 验证工具',
    'C. 包过滤',
    'D. 数据备份'
  ]}
  answer="D"
  explanation="防火墙组成包括服务访问规则、验证工具、包过滤、应用网关，不包括数据备份。"
/>

<ExerciseBox
  question="实时入侵检测的特点是（ ）"
  options={[
    'A. 定期进行检测',
    'B. 发现入侵迹象立即断开连接',
    'C. 由管理人员手动进行',
    'D. 不具有实时性'
  ]}
  answer="B"
  explanation="实时入侵检测在网络连接过程中进行，发现入侵迹象立即断开连接。"
/>

<ExerciseBox
  question="数字签名主要解决以下问题，除了（ ）"
  options={[
    'A. 伪造',
    'B. 抵赖',
    'C. 冒充',
    'D. 加密'
  ]}
  answer="D"
  explanation="数字签名主要用于解决伪造、抵赖、冒充、篡改问题，加密不是其主要目的。"
/>

### 高频考点补充

<ExerciseBox
  question="以下关于对称加密和非对称加密的描述，正确的是（ ）"
  options={[
    'A. 对称加密的加密速度比非对称加密慢',
    'B. 对称加密使用公钥和私钥两个密钥',
    'C. DES是对称加密算法，RSA是非对称加密算法',
    'D. 非对称加密适合加密大量数据'
  ]}
  answer="C"
  explanation="DES是对称加密算法（加密解密用同一密钥），RSA是非对称加密算法（使用公钥私钥对）。对称加密速度快，适合大量数据加密；非对称加密速度慢，适合加密少量数据如密钥。"
/>

<ExerciseBox
  question="数字证书不包含以下哪项内容（ ）"
  options={[
    'A. 证书序列号',
    'B. 证书持有者的公钥',
    'C. 证书持有者的私钥',
    'D. 证书颁发机构的签名'
  ]}
  answer="C"
  explanation="数字证书包含：证书版本、序列号、签名算法、颁发机构、有效期、持有者信息、公钥等信息，但不包含私钥。私钥由证书持有者自己保密存储，绝对不能出现在证书中。"
/>

<ExerciseBox
  question="以下关于防火墙的描述，错误的是（ ）"
  options={[
    'A. 包过滤防火墙工作在网络层和传输层',
    'B. 应用层防火墙可以理解应用层协议',
    'C. 防火墙可以防范所有类型的网络攻击',
    'D. 防火墙不能防止内部网络的恶意攻击'
  ]}
  answer="C"
  explanation="防火墙不能防范所有类型的网络攻击，如不能防范病毒、木马、内部攻击等。防火墙主要用于控制内外网之间的访问，不是万能的安全解决方案。"
/>

<ExerciseBox
  question="VPN技术主要使用的协议是（ ）"
  options={[
    'A. SSL/TLS',
    'B. IPSec',
    'C. SSH',
    'D. 以上都是'
  ]}
  answer="D"
  explanation="VPN（虚拟专用网）可以使用多种协议实现：IPSec是网络层VPN协议，SSL/TLS是应用层VPN协议（如OpenVPN），SSH也可以建立隧道。不同的VPN场景使用不同的协议。"
/>

<ExerciseBox
  question="以下哪种加密算法是对称加密算法（ ）"
  options={[
    'A. RSA',
    'B. ECC',
    'C. AES',
    'D. DSA'
  ]}
  answer="C"
  explanation="AES（高级加密标准）是对称加密算法。RSA、ECC、DSA都是非对称加密算法。常见的对称加密算法有：DES、3DES、AES、RC4等。"
/>

<ExerciseBox
  question="在网络访问控制中，安全性最高但灵活性最差的是（ ）"
  options={[
    'A. 自主访问控制（DAC）',
    'B. 强制访问控制（MAC）',
    'C. 基于角色的访问控制（Role-BAC）',
    'D. 基于规则的访问控制（Rule-BAC）'
  ]}
  answer="B"
  explanation="强制访问控制（MAC）基于安全级别标签，由系统管理员强制执行，用户不能自行更改，因此安全性最高但灵活性最差。DAC灵活性最高但安全性较低。"
/>

## 六、重点总结

### 本节必记考点 ⭐⭐⭐⭐⭐

1. **身份认证方式**：静态密码、智能卡、短信密码、动态口令、USB Key、生物识别、双因素认证
2. **访问控制四机制**：DAC、Role-BAC、Rule-BAC、MAC
3. **防火墙基本特性**：必经之路、策略控制、强抗攻击性
4. **加密两类型**：对称加密（DES）、非对称加密（RSA）
5. **数字签名**：解决伪造、抵赖、冒充、篡改问题
6. **防病毒六策略**：拒绝访问、检测、控制传播、清除、恢复、替代操作
7. **网闸特点**：两套系统之间没有直接的物理通路
